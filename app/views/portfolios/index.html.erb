<div class="album py-5 bg-light">
    <div class="container">

        <%
=begin%>
 <div class="row" id="<%= "draggable" if logged_in?(:site_admin) %>" data-controller="<%= "drag" if logged_in?(:site_admin) %>" data-drag-url="/portfolios/:id/move"> 
<%
=end%>
        <div class="row" id="draggable" data-controller="drag" data-drag-url="/portfolios/:id/move">
            <%= render partial: 'portfolio_item', collection: @portfolio_items %>
            <% @portfolio_items.order(position: :asc) %>
        </div>
    </div>
</div>